# Gateway
OPENCLAW_GATEWAY_TOKEN=generate-with-openssl-rand-hex-32
OPENCLAW_GATEWAY_BIND=loopback
OPENCLAW_GATEWAY_PORT=18789

# Paths
VAULT_DIR=/srv/nazar/vault
OPENCLAW_CONFIG_DIR=/srv/nazar/data/openclaw
OPENCLAW_WORKSPACE_PATH=99-system/openclaw/workspace
OPENCLAW_SRC=/opt/openclaw

# Voice
WHISPER_MODEL=small

# --- Deploy configuration (used by setup-vps.sh) ---
# Base directory for the Nazar installation (default: /srv/nazar)
# NAZAR_ROOT=/srv/nazar

# OS user that owns files and runs cron jobs (default: debian)
# DEPLOY_USER=debian

# External git remote for the vault (GitHub, GitLab, etc.)
# If unset, setup-vps.sh creates a local bare repo at $NAZAR_ROOT/vault.git
# VAULT_GIT_REMOTE=git@github.com:youruser/vault.git
