# =============================================================================
# Nazar Second Brain - Environment Configuration
# Single debian user + Docker containers setup
# =============================================================================

# =============================================================================
# Deployment Mode
# =============================================================================

# Choose your deployment mode:
# - "tailscale": Use Tailscale VPN for secure access (recommended for multi-device)
# - "sshtunnel": Use SSH tunnel for gateway access (simpler, single-user)
# - "localhost": Direct localhost access (testing only)
DEPLOYMENT_MODE=tailscale

# =============================================================================
# Host Paths (where data is stored - under debian user)
# =============================================================================

# Base directory for all persistent data
NAZAR_BASE_PATH=/home/debian/nazar

# Main vault directory - shared between OpenClaw and Syncthing
VAULT_HOST_PATH=/home/debian/nazar/vault

# OpenClaw configuration directory
OPENCLAW_CONFIG_PATH=/home/debian/nazar/.openclaw

# OpenClaw workspace directory (skills, memory, etc.)
OPENCLAW_WORKSPACE_PATH=/home/debian/nazar/.openclaw/workspace

# Syncthing configuration directory
SYNCTHING_CONFIG_PATH=/home/debian/nazar/syncthing/config

# Tailscale state directory (if using Tailscale mode)
TAILSCALE_STATE_PATH=/home/debian/nazar/tailscale

# =============================================================================
# OpenClaw Gateway Configuration
# =============================================================================

# Gateway bind address
# - "0.0.0.0" for all interfaces (with Tailscale or behind firewall)
# - "127.0.0.1" for SSH tunnel only (most secure)
OPENCLAW_GATEWAY_BIND=0.0.0.0

# Gateway port
OPENCLAW_GATEWAY_PORT=18789

# Gateway token (auto-generated if empty)
# OPENCLAW_GATEWAY_TOKEN=your-secure-token-here

# =============================================================================
# Tailscale Configuration (when DEPLOYMENT_MODE=tailscale)
# =============================================================================

# Hostname for this device in Tailscale network
TAILSCALE_HOSTNAME=nazar

# Auth key for unattended setup (recommended for VPS)
# Generate at: https://login.tailscale.com/admin/settings/keys
# Create a reusable, ephemeral key
TAILSCALE_AUTHKEY=tskey-auth-xxxxxxxxxxxxxxxx-xxxxxxxxxxxxxxxx

# =============================================================================
# SSH Tunnel Configuration (when DEPLOYMENT_MODE=sshtunnel)
# =============================================================================

# Bind gateway to localhost only (access via SSH tunnel)
# SSH_TUNNEL_PORT=18789

# =============================================================================
# LLM API Keys (optional - can also configure via OpenClaw CLI)
# =============================================================================

# Anthropic Claude API key
# ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxx

# OpenAI API key (if using OpenAI models)
# OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx

# =============================================================================
# Syncthing Configuration (optional)
# =============================================================================

# GUI username (optional - set for auto-configuration)
# SYNCTHING_GUI_USER=admin

# GUI password (optional - set for auto-configuration)
# SYNCTHING_GUI_PASS=your-secure-password

# =============================================================================
# OpenClaw Build Options
# =============================================================================

# Extra apt packages to install in OpenClaw container (space-separated)
# Examples: "chromium build-essential libpq-dev ffmpeg"
# OPENCLAW_EXTRA_PACKAGES=ffmpeg

# =============================================================================
# Advanced Options
# =============================================================================

# Container user ID (should match debian user, usually 1000)
CONTAINER_UID=1000
CONTAINER_GID=1000

# Log retention (days)
LOG_RETENTION_DAYS=7
